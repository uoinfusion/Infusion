<UserControl x:Class="Infusion.Desktop.OptionsControl"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <StackPanel Margin="10 10 10 10">
        <CheckBox Content="Show toast notification for conversation (requires Windows 10)" IsChecked="{Binding Path=ConversationToastNotificationEnabled}" IsEnabled="{Binding Path=CanShowToastNotification}" />
        <CheckBox Content="Show toast notification for alerts (requires Windows 10)" IsChecked="{Binding Path=AlertToastNotificationEnabled}" IsEnabled="{Binding Path=CanShowToastNotification}" />
        <CheckBox Content="Hide to tray when minimized" IsChecked="{Binding Path=HideWhenMinimized}" />
    </StackPanel>
</UserControl>
